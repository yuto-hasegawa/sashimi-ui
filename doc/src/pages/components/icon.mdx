---
title: Icon
layout: ../../components/Layout.astro
---

import SamplePane from "../../components/SamplePane.astro";

# Icon

`.icon` adjusts its own appearance to fit the surrounding context.

## Applicable Elements

- `svg`
- `img`
- `picture`

## Basic Usage

`.icon` sits well in text elements such as `<p>` or `<h1>`.  
It also works seamlessly alongside other sashimi UI classes like <a href={`${import.meta.env.BASE_URL}components/button/`}>`.button`</a>, <a href={`${import.meta.env.BASE_URL}components/link/`}>`.link`</a> and <a href={`${import.meta.env.BASE_URL}components/menu-item/`}>`.menu-item`</a>.


<SamplePane>
<h2 style="margin: 0;"><svg class="icon" stroke="currentColor"><use href={`${import.meta.env.BASE_URL}diamond.svg#root`} /></svg>Heading with an icon</h2>
<button class="button"><img class="icon" src={`${import.meta.env.BASE_URL}delete_blue.svg#root`} alt=""/>Delete</button>
<p style="margin: 0;"><picture class="icon"><source srcset={`${import.meta.env.BASE_URL}saved_white.svg#root`} media="(prefers-color-scheme: dark)"/><img src={`${import.meta.env.BASE_URL}saved_black.svg#root`} alt="" aria-hidden="true"/></picture>Settings have been saved.</p>
</SamplePane>

```html
<h2>
  <svg class="icon"><use href="/diamond.svg#root" /></svg>
  Heading with an icon
</h2>

<button class="button">
  <img class="icon" src="/delete_blue.svg#root" alt=""/>
  Delete
</button>

<p>
  <picture class="icon">
    <source srcset="/saved_white.svg#root" media="(prefers-color-scheme: dark)"/>
    <img src="/saved_black.svg#root" alt="" aria-hidden="true"/>
  </picture>
  Settings have been saved.
</p>
```

## Tinting Icons

sashimi UI does **NOT** provide a built-in icon recoloring feature.  
we recommend using SVGs for icon coloring.

Prepare SVGs that have no fixed width/height, and whose fill or stroke are `currentColor`.  
Make sure it has an element with an `id`.  
Then reference them with `<use>` so the icon inherits color from CSS.

```xml
<!-- icon.svg -->
<svg
  xmlns="http://www.w3.org/2000/svg"
  id="root"
  viewBox="0 -960 960 960"
  fill="currentColor"
>
  <path d="..."/>
</svg>
```

```html
<!-- index.html -->
<button class="button">
  <svg class="icon">
    <title>alt text</title>
    <use href="/icon.svg#root" />
  </svg>
  Button Text
</button>
```

<SamplePane>
  <button class="button primary">
    <svg class="icon">
      <title>Red Icon</title>
      <use href={`${import.meta.env.BASE_URL}diamond.svg#root`} />
    </svg>
    Primary
  </button>
  
  <button class="button secondary">
    <svg class="icon">
      <title>Red Icon</title>
      <use href={`${import.meta.env.BASE_URL}diamond.svg#root`} />
    </svg>
    Secondary
  </button>
  
  <button class="button tertiary">
    <svg class="icon">
      <title>Red Icon</title>
      <use href={`${import.meta.env.BASE_URL}diamond.svg#root`} />
    </svg>
    Tertiary
  </button>
  
  <button class="button tertiary" style="color: salmon;">
    <svg class="icon">
      <title>Red Icon</title>
      <use href={`${import.meta.env.BASE_URL}diamond.svg#root`} />
    </svg>
    Custom color
  </button>
</SamplePane>